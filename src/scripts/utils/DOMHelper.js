/**
 * A utility class which provides simple
 * convenience methods for manipulating the DOM
 * using vanilla JavaScript.
 */
export default class DOMHelper {
  static get(name) {
    const isId = name.indexOf('#') > -1;
    const isClass = name.indexOf('.') > -1;

    debugger;
  }
}